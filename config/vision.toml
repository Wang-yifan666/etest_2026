[vision]
red_h1_min = 0
red_h1_max = 10
red_h2_min = 170
red_h2_max = 180
saturation_min = 100
value_min = 80
min_area = 200.0
morphology_kernel = 5

[vision.ball]
# ── 工作分辨率 ──
work_width = 640
work_height = 480

# ── 搜索 ROI（工作分辨率内坐标）──
roi_x = 60
roi_y = 180
roi_w = 520
roi_h = 100

axis_x1 = 80.0
axis_y1 = 230.0
axis_x2 = 560.0
axis_y2 = 230.0
axis_length_cm = 25.0

# ── 棕色管道 HSV 阈值 ──
brown_h_min = 5
brown_h_max = 30
brown_s_min = 30
brown_s_max = 255
brown_v_min = 30
brown_v_max = 255

# ── 管道形态学 ──
pipe_close_kernel_w = 31
pipe_close_kernel_h = 9
pipe_open_kernel = 3

# ── 管道几何约束 ──
pipe_min_area_ratio = 0.05
pipe_min_aspect_ratio = 3.0
pipe_min_fill_ratio = 0.30
pipe_horizontal_angle_max = 25.0

# ── 管道锁定 ──
pipe_stable_frames = 8
pipe_lost_timeout_frames = 30

# ── 透视展开 ──
pipe_warp_width = 500
pipe_warp_height = 120
pipe_inner_margin_x_ratio = 0.03
pipe_inner_margin_y_ratio = 0.10

# ── 管道实际长度(mm) ──
pipe_length_mm = 250.0

# ── 钢球检测参数 ──
bg_kernel = 31
threshold = 18
morph_kernel = 3

min_area = 40.0
max_area = 3000.0
min_circularity = 0.35
ball_min_aspect = 0.65
ball_max_aspect = 1.45
ball_min_local_contrast = 8.0
ball_max_centerline_distance_px = 35.0
max_axis_distance_px = 30.0
max_jump_px = 50.0

reacquire_after_lost_frames = 15

zero_mode = "startup"
zero_position_px = 240.5
zero_samples = 20
zero_range_px = 4.0

filter_alpha = 0.35

# 旧参数保留（待迁移）
white_s_max = 50
white_v_min = 160
track_min_area_ratio = 0.05
track_min_aspect_ratio = 4.0
track_stable_frames = 10
track_lost_timeout_frames = 60
track_search_roi_x = 0
track_search_roi_y = 220
track_search_roi_w = 640
track_search_roi_h = 220
track_horizontal_angle_max = 20.0